# ДЗ №1. Изучение средств защиты ОС GNU/Linux

**Студент:** Анастасия Тобохова
**Уникальный номер:** 372772
**ОС:** Fedora Workstation, виртуальная машина UTM.

## Выполнение задания

| Раздел                   | Выполненные действия                                                                                                                                                                              |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Создание пользователя | Создан пользователь `user1` с UID `1234` и основной группой `students`. Максимальный срок действия пароля — 90 дней.                                                                              |
| 2. Мониторинг            | Выполнен поиск файлов с битом set-UID и процессов с эффективным UID `0`, запущенных обычными пользователями.                                                                                      |
| 3. set-UID               | Утилита `cat` скопирована в `/home/user1/cat`. Владелец — `root`, права — `4755`. Проверено чтение `/etc/shadow` от имени `user1`: обычный `cat` получает отказ, копия с set-UID читает файл.     |
| 4. Привилегии            | Утилита `chown` скопирована в `/home/user1/chown`, назначена capability `cap_chown=ep`. От имени `user1` успешно изменён владелец `/home/user1/testfile` на `root`.                               |
| 5. sudo                  | В `/etc/sudoers` добавлено правило `user1 ALL=(root) /usr/bin/date`, разрешающее изменение системного времени. Синтаксис проверен командой `visudo -c`, разрешение — командой `sudo -l -U user1`. |

Административные команды выполнялись из учётной записи `anastasia`. Для проверок использовалась учётная запись `user1`, в том числе запуск отдельных команд через `sudo -u user1`.

## Артефакты

| Файл                   | Содержимое                                                     |
| ---------------------- | -------------------------------------------------------------- |
| `mephi-screenshot.png` | Скриншот терминала с уникальным номером студента               |
| `history.out`          | История выполненных команд                                     |
| `stat.out`             | Метаданные `/home/user1` и скопированных утилит `cat`, `chown` |
| `getcap.out`           | Назначенная привилегия `cap_chown=ep`                          |
| `passwd`               | Копия `/etc/passwd`                                            |
| `shadow`               | Данные `/etc/shadow`                                           |
| `group`                | Копия `/etc/group`                                             |
| `sudoers`              | Копия `/etc/sudoers`                                           |
| `suid-files.out`       | Результат поиска set-UID-файлов                                |
| `privileged-proc.out`  | Результат мониторинга процессов с повышенными привилегиями     |
